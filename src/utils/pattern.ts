export const alphaNumericPattern = /^[a-zA-Z0-9]+(?:\s[a-zA-Z0-9]+)*$/;

export const Patterns = {
  ALPHA_NUMBER_ONLY: /^[a-zA-Z0-9]+(?:\s[a-zA-Z0-9]+)*$/,
  NUMBER_ONLY: /^[0-9]+$/,
  FLOAT_NUMBER_ONLY: /^[0-9]+(\.[0-9]+)?$/,
};
